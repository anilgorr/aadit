Using the ServicePageTemplate from Phase 1 and the MDX "services" content collection
from Phase 0, create MDX content files and pages for every page in this list. Each MDX
file needs frontmatter: title, metaDescription, slug, hub (cybersecurity/compliance/
it-managed-services), publishedAt, updatedAt.

Cybersecurity hub (/cybersecurity/) + children:
managed-soc, soc-as-a-service, vapt, endpoint-security, firewall-network-security, consulting

Compliance hub (/compliance/) + children:
iso-27001, iso-42001, iso-9001, gdpr, pci-dss, hipaa, soc2

IT Managed Services hub (/it-managed-services/) + children:
cloud-infrastructure, cloud-migration, cloud-optimization, helpdesk-support,
backup-disaster-recovery

I'll provide the actual exported copy for each corresponding old page (or pages, where
several old URLs are merging into one) — reuse that original wording as closely as
possible rather than rewriting it. Restructure it into the new template's sections
(definition opener, benefits, what's included, FAQ) without changing the substance of
what's already there. Two exceptions, called out per-page where they apply: (1) if a
page's "Why Choose Us" or closing section doesn't actually match that page's topic
(this happened at least once in the current site — the Firewall page's closing section
was copy-pasted from the Cloud Infrastructure page), replace it with something that
actually describes this service, don't carry the mismatch forward; (2) if a page's
testimonial or pricing content is generic/placeholder (unattributed quotes like "– IT
Company," round-number pricing tiers with no real backing), flag it for me instead of
publishing it as-is — either I'll provide the real version or we drop that block.

Each hub page (e.g. /cybersecurity/) should list and link to its children with short
descriptions. Each child page needs: the definition-style opening paragraph already
present in the source content (tighten to 40-60 words if it's longer — this matters for
AI search extraction, I'll explain in Phase 5), the benefits section, "what's included"
list, and FAQ section from the source, plus internal links to 2-3 related pages in other
hubs (e.g. the HIPAA compliance page should link to Managed SOC and Endpoint Security).

Where multiple old pages are merging into one (Managed SOC absorbing the separate
"managed-soc" and "security-as-a-service-india" pages; ISO 27001 absorbing the separate
"iso-27001-consulting-india" page; IT Managed Services absorbing three old URLs), combine
the strongest original content from each source page rather than picking just one and
discarding the rest — but don't duplicate the same point twice if both sources say
essentially the same thing.

Do NOT create separate near-duplicate pages for concepts that overlap (e.g. don't create
both a "security as a service" and "SOC as a service" page unless the content is
genuinely distinct) — one canonical page per topic only.